CREATE TABLE bears
    -- (id INTEGER PRIMARY KEY,
    (id INTEGER PRIMARY KEY,
    name TEXT,
      age INTEGER,
      gender char(1),
      color text,
      temperament	text,
      alive INTEGER
    -- grade INTEGER,
    -- gpa FLOAT,
    -- tardies INTEGER

  );
